enum EmailValidationEnum { initial, empty, invalid, valid }

enum NameValidationEnum { initial, empty, invalid, valid }

enum UserNameValidationEnum { initial, empty, invalid, valid, exist, available }

enum UrlValidationEnum { initial, empty, http, invalid, valid }
